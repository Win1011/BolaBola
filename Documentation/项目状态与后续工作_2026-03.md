# BolaBola 项目状态与后续工作（2026 年 3 月）

本文汇总近期为解决 **会员/证书限制、同步、语音、分析** 等做的改动，以及**已知技术债**与推荐优化方向。更细的 App Group 说明见同目录 [`app_group_removal_and_restore.md`](./app_group_removal_and_restore.md)；手表安装问题见 [`watch_app_install_troubleshooting.md`](./watch_app_install_troubleshooting.md)。  
**iPhone 产品需求（三板块 + 双数值）** 见 [`PRD_iPhone_三板块_2026-04.md`](./PRD_iPhone_三板块_2026-04.md)；手表 v1 范围仍见 [`BolaBola Watch App/Documentation/PRD_v1.md`](../BolaBola%20Watch%20App/Documentation/PRD_v1.md)。

---

## 1. App Group：去掉又加回来的背景

- **动机**：Personal Team 等场景下，entitlements 里声明了 App Group，但 provisioning profile 未包含对应 group 时，可能导致 **手表伴侣 App 安装失败**。曾尝试 **从 entitlements 移除 App Group**，让 iPhone / Watch 各用 `UserDefaults.standard`，避免 profile 与声明不一致。
- **代价**：无 group 时，两端 **不共享** 同一套 `UserDefaults`；陪伴「游戏状态」等依赖共享 defaults 的能力，改为主要靠 **WatchConnectivity**（如手表 → iPhone 的 `csV1` 快照）补偿。
- **当前**：若已恢复 entitlements 中的 App Group 且账号侧能力配齐，则 `BolaSharedDefaults.resolved()` 会再次优先使用 `group.com.gathxr.BolaBola`。
- **后续**：在 `AppGroupConfig` / `BolaSharedDefaults` 中搜索 **`RESTORE_APP_GROUP_WHEN_PAID_DEV`** 可定位相关注释；续费正式开发者后应 **统一 entitlements、Developer Portal 里的 App Group、与 profile**，并做一次 **双端读写 + WC** 回归。

---

## 2. WatchConnectivity 与「LLM 配置」同步

- **设计**：iPhone 在 Keychain 中保存对话 API（Key / Base URL / 模型等），通过 `transferUserInfo` 同步到手表 Keychain（`ingestLLMConfigurationIfPresent`）。
- **问题**：同步链路不稳定或时序问题时，手表会出现 **`loadFromKeychain: missing API key`**，语音直连智谱无法发起。
- **已加补偿**：
  - 手表在 WC 激活后、以及伴侣 App 状态变化时，若本地无 Key，会发 **`requestSync = llmKeychain`**，请 iPhone 再执行 `pushStoredLLMConfigurationToWatchIfConfigured()`。
  - 日志（subsystem `com.gathxr.BolaBola.sync`，category `WatchConnectivity`）中可搜 `ingestLLMConfiguration`、`pushStoredLLMConfigurationToWatchIfConfigured skip`。
- **后续优化**：恢复可靠同步后，可弱化或保留「拉取」作兜底；考虑在设置页增加「仅向手表推送配置」按钮；长期应用 **登录账号 + 云端下发密钥** 替代明文 WC 传 Key。

---

## 3. API / LLM：当前是「多来源、需统一」

| 来源 | 用途 | 说明 |
|------|------|------|
| **Keychain**（iPhone 设置里保存） | 主路径 | 优先；可同步到表。 |
| **`Shared/LLM/LocalLLMDevSecrets.swift`** | 开发兜底 | Keychain 无有效 Key 时使用；**`apiKey` 提交仓库时应为空**，本地自填，**勿向公开仓库推送含真 Key 的提交**。 |
| **智谱 `open.bigmodel.cn`** | 手表录音 ASR + 对话 | `LLMClient.transcribeAudio` 仅支持智谱域；Base URL 需为 `…/api/paas/v4` 类路径。 |
| **默认 OpenAI 地址** | 未保存 Base URL 时的默认 | 与智谱 Key 混用会 401；设置里务必保存正确 Base URL。 |

- **日志**：手表语音调试可过滤 category **`WatchVoice`**（`transcribeAudio`、`replyFromAudio`、`toggleMic` 等）。
- **后续优化**：统一「单一配置源」策略（仅 Keychain 或仅云端）；上架前移除或条件编译 `LocalLLMDevSecrets`；ASR 与 chat 的模型字段可拆分配置。

---

## 4. Firebase（Analytics）

- 已通过 **Swift Package Manager** 引入 **Firebase iOS SDK**，iOS 目标链接 **`FirebaseAnalyticsCore`**（无 IDFA 路径）；**手表目标未链 Analytics**（二进制产物不支持 watchOS）。
- 根目录 **`GoogleService-Info.plist`** 已加入 **iOS + Watch** 的 Copy Bundle Resources（当前主要为 iOS 使用）；若 Watch 单独注册 Firebase 应用，需控制台单独配置。
- 初始化：`FirebaseApp.configure()` 在 **`IOSAppDelegate`**（`BolaBola iOS/App/IOSNotificationRouter.swift`）的 `didFinishLaunching` 中调用。
- **后续**：按需加 Crashlytics / Remote Config；Watch 若要用 Firebase 非 Analytics 能力，按官方文档单独选 product。

---

## 5. 手表语音链路（简要）

1. 手表 **麦克风** → `WatchSpeechRelay`（录音 WAV）→ **`ConversationService.replyToUserFromRecordedAudio`** → 智谱 **ASR** → **chat** → `PetViewModel` 播放回复。
2. 云端 ASR 失败时，仍会尝试 **iPhone 中继转写**（`transferFile` + `IOSpeechRelayTranscriber`），不依赖手机时的优化见第 3 节（配置与网络）。
3. `BolaSpeechCapture` 在 watchOS 上为 **无 Speech 框架的占位**，真正识别不走系统 Speech。

---

## 6. 工程目录整理（2026-03）

为侧边栏清晰，代码已按职责分子文件夹（**不**把超大文件硬合并）：

- **`Shared/Defaults/`**：共享 UserDefaults / App Group 相关、Keychain 助手、陪伴持久化键。
- **`Shared/Sync/`**：`BolaWCSessionCoordinator`、`WCSyncPayload`、聊天记录模型与合并逻辑。
- **`Shared/LLM/`**：`LLMClient`、`LLMModels`、`ConversationService`、`LocalLLMDevSecrets`。
- **`Shared/Companion/`**：`CompanionTier`。
- **`Shared/Reminders/`**、**`Shared/Digest/`**：提醒与每日总结相关。
- **`BolaBola iOS/App/`**、**`Features/*`**、**`Design/`**：入口、功能分区、主题与 mock。
- **`BolaBola Watch App/App/`**、**`Views/`**、**`Speech/`**、**`Pet/`**、**`Health/`**、**`Reminders/`**、**`Documentation/`**。

手表端 **`WatchSpeechRelayRecorder` + `WatchSpeechRelayCapture`** 合并为同一文件 **`Speech/WatchSpeechRelay.swift`**（两类型仍分开，总行数适中，便于阅读）。

Xcode 使用 **File System Synchronized** 的文件夹，磁盘上移动文件后工程会自动收录，**一般无需改 `project.pbxproj`**。

---

## 7. 建议的后续优先级（备忘）

1. **同步与配置**：稳定 WC + Keychain 或改为账号 + 云端密钥；减少对手写 `LocalLLMDevSecrets` 的依赖。  
2. **App Group**：与付费开发者账号对齐后，全文测 **共享 defaults + 快照 + WC** 是否重复或冲突。  
3. **提醒功能**：在现有 Reminder / Health 模块上继续产品化（与语音、陪伴值联动等）。  
4. **安全与上架**：密钥管理、隐私政策、Sign in with Apple（若上登录）等。  
5. **结构**：新增文件时优先放入上面对应子目录，避免再回到扁平大列表。

---

## 8. 生活 Tab 布局、滚动边缘与 GitHub `main` 对齐说明

- **根级 `TabView`（iOS 26）** 使用 `tabBarMinimizeBehavior(.onScrollDown)` 时，系统会把 **当前 Tab 的主纵向 `ScrollView`** 与底栏收起关联。
- **若用横向分页 `ScrollView` 或内层分页 `TabView`（`.page`）包住「生活 / 时光」**：容易出现 **纵向滚动时底栏不再收起**，且分页容器可能 **改变安全区**、与 Figma 全幅背景不一致。当前采用 **`switch(lifeSegment)` + 两列各自纵向 `ScrollView`**，由导航栏 `IOSLifeSegmentLarge` 切换，无左右滑。
- **滚动边缘**：根视图与各页通过 `scrollEdgeEffectStyle`（见 `IOSGlassChrome.swift`）统一顶/底质感；与 [Win1011/BolaBola](https://github.com/Win1011/BolaBola) 上 `IOSRootView` / `IOSGlassChrome` 若一致，差异多半在 **`IOSLifeContainerView` 的滚动层级**与功能迭代（记录类型、天气卡等），而非神秘「UI 漂移」。
- **底栏 `TabSection` 与「对话」之间的间距、三格胶囊宽度**：由系统 Liquid Glass Tab 绘制，**无稳定公开 API** 微调；导航栏「生活｜时光」标题区可单独压字号/间距（`IOSLifeToolbarCenter.swift`）。

---

*文档随迭代更新；若与代码不一致，以仓库内实现为准。*

---

## 0409 更新：功能现状全量盘点与后续计划

> 更新日期：2026-04-09  
> 基于：主目录代码全量扫描 + Bolalevel.md / Bolatitle.md / roadmap_to_0420.md

### 已完成（无需动）

| 模块 | 说明 |
|------|------|
| Watch 宠物状态机 | 情绪/动画/对话/语音，完整 |
| 陪伴值计分 | 墙上时钟累加 + 自动扣罚 + 分级映射 |
| **时间型等级加速** | companion ≥ 80 时 Watch 打开时长 × 1.2 倍结算 XP，`BolaGrowthSystem.grantOpenAppXP` 已实现 |
| Watch 端打开时长追踪 | `openAppGrowthSessionStartedAt` 记录进入/退出前台，回调 `grantOpenAppXP` |
| WatchConnectivity 双向同步 | 陪伴值/聊天/LLM配置/语音中继，完整 |
| iOS 4 Tab + Liquid Glass | 主界面/成长/生活/对话，完整 |
| LLM 对话 | HTTP 客户端 + 智谱 ASR + Alarm 解析，完整 |
| 提醒系统 | 创建/编辑/删除 + UNUserNotifications 持久化 |
| 每日摘要通知 | 9AM 健康摘要，完整 |
| HealthKit 周视图 + HRV | 完整 |
| Firebase 初始化 | SDK 已接入，无埋点 |
| Watch Widget | 完整 |
| **`BolaGrowthSystem.swift`** | 三段等级公式、XP 引擎（任务/对话/睡眠/开屏时长）、里程碑一次性奖励、防重复、每日 08:00 重置 |
| **`TitleUnlockSystem.swift`** | 全量词库（A+B 池，6类）、解锁条件评估器、持久化、合并逻辑 |
| **`BolaLevelGate`** | 等级门控：Lv0 锁对话 / Lv1–2 学话 / Lv3 全开 / Lv5 性格 / Lv10 隐藏台词 |
| 性格枚举 + Lv5 随机分配 | `BolaPersonalityType` 四种，Lv5 里程碑时随机写入持久化 |

---

### 有骨架、逻辑待补（优先最高）

**T1：每日任务接真实进度**  
文件：`GrowthDailyTaskModels.swift`  
现状：`placeholderProgress()` 返回假值（walk=0.42, chat=0.18）  
要做：`walk` 任务接 HealthKit `queryTodaySteps()`；`chat` 任务读 `ChatHistoryStore` 今日 turns 数（≥1 次即 1.0）

**T2：时光日记持久化**  
文件：`IOSLifeTimePageView.swift`  
现状：硬编码 3 条假数据；`LifeRecordCard` 需补 `createdAt: Date` 字段  
要做：`IOSLifeTimePageView` 改为从 `LifeRecordListStore` 读取（与 T8 共用数据源），FAB 新增记录写入

**T3：性格接入 ConversationService**  
文件：`Shared/LLM/ConversationService.swift`  
现状：`personalityType` 已持久化，未拼入 system prompt  
要做：读取后按四种性格追加对应 prompt 描述，同步到 Watch 语音路径

**T4：Lv0–2 对话学话模式**  
文件：`Shared/LLM/ConversationService.swift`  
现状：`BolaLevelGate.SpeechMode` 枚举已有，未实际拦截  
要做：Lv0 锁对话入口；Lv1 追加「结巴/幼稚」prompt；Lv2 追加「进步中」prompt；Lv3+ 标准

---

### 完全未实现

**T5：首次启动引导流**  
新建 `BolaBola iOS/Features/Onboarding/IOSOnboardingView.swift`  
3–4 屏：欢迎 → 通知 → HealthKit → 定位（天气）；完成写标记 `bola_onboarding_done_v1`；所有权限可跳过

**T6：LLM 未配置空状态**  
`IOSChatTestSection.swift`：检查 Keychain 是否为空，空则显示「前往设置配置 AI 密钥」引导卡

**T7：Watch 未配对提示**  
`IOSMainHomeView.swift`：检查 `WCSession.isPaired / isWatchAppInstalled`，未配对显示说明卡

**T8：生活卡片联动（对话 → 自动生成卡片）**  
核心思路：用户与 Bola 的对话，LLM 提取后自动生成 `LifeRecordCard`；手动添加的卡片反向注入 Bola 上下文；卡片沉淀为时光日记数据源。  
数据流：对话 → LLM 标签提取（`<<LIFE:kind|title|detail>>`，参考现有 `AlarmIntentParser`）→ `LifeCardIntentParser` → `LifeRecordListStore`；`ConversationService` system prompt 附加「今日生活摘要」（最近 5 条）。  
卡片类型映射：「和朋友吃饭」→ `.event`；「吃了XX饭店」→ `.food`；「去爬山」→ `.fitness`；「看了部电影」→ `.movie`；「去XX旅游」→ `.travel`  
注意：`LifeRecordCard` 需补 `createdAt: Date` 字段；提取失败不影响正常对话

**T9：生活泡泡可视化**  
入口：`lifeBubbleMode` 绑定已通，`IOSLifeContainerView` 有 `@Binding var bubbleMode` 但 body 无分支逻辑（文件中已有 `BubbleTail` Shape 残留）  
要做：`bubbleMode = true` 时显示全屏圆形泡泡视图，每个 `LifeRecordCard` 变成一个气泡（颜色按 kind 区分），点击展开详情；数据源与 T8 共用 `LifeRecordListStore`

---

### 已设计、等待扩展

| 功能 | 优先级 |
|------|--------|
| 升级动画反馈（XP 条满 → Bola 庆祝动画 + 台词） | P1 |
| 称号图鉴 UI 分区展示（已解锁彩色/未解锁灰+条件/稀有问号） | P1 |
| 睡眠 XP 接 HealthKit（`grantSleepXP()` 入口已有，未调用） | P1 |
| 节日类称号（除夕/圣诞/元旦/生日限时触发） | P2 |
| 稀有类称号（凌晨开 App、满分日等隐藏触发） | P2 |
| Firebase 埋点（SDK 已初始化，无任何 `logEvent()`） | P2 |
| Streak 连续天数奖励（7天/30天 XP） | P2 |
| 表盘组件拖拽自定义 | P3（v1.1） |
| App Group 恢复 | P3（正式账号后） |

---

### 建议执行顺序

**4/20 前（TestFlight 必须）**：T1 → T3 → T4 → T6 → T7 → T5 → T2  
**4/20 后（体验深度）**：T8（生活卡片联动）→ T9（泡泡视图）→ 升级动画 → 称号图鉴 UI

---

## 0411 同步：桌面 0409 roadmap + 当前本地代码对齐

> 更新日期：2026-04-11  
> 同步来源：`/Users/xinyuewang/Desktop/roadmap_dual_value_0409.md` + 当前本地代码状态。  
> 说明：上方「0409 更新」保留为历史盘点；本节为最新状态覆盖。

### 已完成首版

| 模块 | 当前状态 |
|------|----------|
| **T1 每日任务接真实步数** | `GrowthDailyTasksViewModel.refreshWalkProgress()` 已接今日步数；聊天任务读 `dailyIOSChatCount` |
| **T3 性格接 ConversationService** | 已接入；后续首版策略改为 Lv5 只解锁傲娇 |
| **T4-LLM Lv0–2 学话模式** | `ConversationService` 按等级拼接说话规则 |
| **T4-UI Chat/Watch Lv0 拦截** | iOS Chat 有 Lv0 锁定空状态；Watch 抽屉麦克风按 `canDialogue` 控制显示 |
| **T5 首次启动引导流** | `IOSOnboardingView` + `BolaOnboardingState` + `IOSRootView.fullScreenCover` 已接入 |
| **T6 LLM 未配置空状态** | iOS Chat onAppear 检查 Keychain / dev key；未配置时显示前往设置配置引导 |
| **T7 Watch 未配对提示** | `IOSMainHomeView` 已显示 Watch 未配对 / 未安装 App 提示 |
| **傲娇人格首版** | Lv5 首版只解锁傲娇；设置页可切「默认 / 傲娇」；iPhone 对话、Watch 录音主链路、Watch fallback 统一使用人格 prompt |
| **时光日记（对话自动生成版）** | 新增 `Shared/Diary`；聊天后后台提取 `BolaDiaryEntry`；时光页按日期展示自动生成日记；手动记录入口已移除 |
| **T8 生活卡片联动** | 对话后台提取可生成结构化 `LifeRecordCard`；最近生活卡片注入 Bola 对话上下文 |
| **T9 生活泡泡可视化** | 生活页 bubble mode 下显示可点击泡泡，并可打开详情 sheet |
| **iOS Chat 体验增强** | 渐变背景、语音输入、动态波形录音提示、时间戳/日期分隔、发送中/失败重试、长按菜单、聊天日历、回到最新、自动记忆提示 |
| **Watch idle5 点击恢复** | 针对 idle5 时点击不跳跃的问题，增加 stale tap lock 恢复判断 |

### 仍待做 / 待打磨

| 模块 | 说明 |
|------|------|
| **升级动画反馈** | XP 条满 → 升级弹出层，Bola 播放庆祝动画 + 台词 |
| **称号图鉴 UI 分区展示** | 已解锁彩色 / 未解锁灰色+条件 / 稀有「？？？」 |
| **Onboarding UI 设计打磨** | 当前引导流功能已接入；首次启动视觉语言、权限解释、品牌化动效与转场仍需重新设计 |
| **特殊动画图鉴丰富** | 成长图鉴需扩充特殊动画条目、解锁条件、预览动效与未解锁提示 |
| **节日类称号触发逻辑** | `holiday` 类型存在，当前日期触发与活动配置未接 |
| **稀有类称号触发逻辑** | 凌晨开 App、满分日、连续狂点等隐藏触发条件未实现 |
| **睡眠 XP 接 HealthKit** | `grantSleepXP()` 入口已有，尚未接 HealthKit 睡眠样本 |
| **Firebase 埋点** | SDK 已初始化，仍未加业务 `logEvent()` |
| **Streak 连续天数奖励** | 7 天 / 30 天 XP 奖励未实现 |
| **Watch 端成长数据镜像** | Watch 显示等级 / 称号，数据同步基础已具备，UI 未接 |
| **每日摘要通知升级** | 可加入今日 XP、等级进度与留存文案 |
| **时光日记编辑 / 删除** | 自动生成与展示已做；删除 / 编辑仍待补 |
| **T8/T9 打磨** | 生活卡片自动添加的 prompt / 触发阈值仍需调教，很多对话内容还没有主动沉淀为卡片；生活提取确认 / 撤销、泡泡避让布局、动效细节仍待优化 |
| **远程配置 / 在线运营内容更新** | 后续用于节日活动、限时称号、奖励参数、活动通知、prompt 片段等远程 JSON / 配置服务下发；App 本地缓存并兜底默认配置，避免纯内容更新重新发版 |
| **订阅内容解锁机制** | 考虑后续用订阅解锁高级内容，例如特殊动画、稀有图鉴、节日活动奖励、更多人格/主题等；需先设计免费/付费边界、StoreKit 接入与本地权限兜底 |

### 最新建议执行顺序

**短期收口（TestFlight 前）**：Chat UI 真机/模拟器视觉回归 → 时光日记 / T8 自动提取手测 → Watch 语音与 Lv0 门控回归 → 隐私政策确认第三方 LLM/语音说明。  
**体验深度（4/20 后）**：Onboarding UI 设计打磨 → 升级动画反馈 → 称号图鉴 UI + 特殊动画图鉴丰富 → 时光日记编辑/删除 + 对话卡片自动添加调教 + 生活卡片撤销/确认 → 节日/稀有称号 → 远程配置/在线运营内容更新 → 订阅内容解锁机制。
